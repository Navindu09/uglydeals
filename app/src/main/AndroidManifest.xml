<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.nothingugly.uglydeals">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <supports-screens
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ugly_deals_icon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-9409818967408705/3461091184" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBlHlia2Rh78eXZegI0Rhlr6VIyoeQl4i8" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".PhoneSetupActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".RegisterActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".LogInActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".SelectedItemActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ScanActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".RedeemSuccessActivity"
            android:launchMode="singleTask" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>